# 智慧农业小程序前端开发文档

## 目录
1. [项目概述](#项目概述)
2. [技术栈](#技术栈)
3. [项目结构](#项目结构)
4. [页面设计](#页面设计)
5. [组件设计](#组件设计)
6. [WebSocket通信](#WebSocket通信)
7. [API接口文档](#API接口文档)
8. [开发规范](#开发规范)
9. [部署说明](#部署说明)

## 项目概述

本项目是一个智慧农业监控与控制小程序，通过微信小程序平台实现对智慧农业环境数据的监控、查询和设备控制。用户可以通过自然语言提问获取环境数据，并控制相关设备。

<img src="C:\Users\25685\Pictures\Screenshots\屏幕截图 2025-04-02 113210.png" alt="屏幕截图 2025-04-02 113210" style="zoom:25%;" /><img src="C:\Users\25685\Pictures\Screenshots\屏幕截图 2025-04-02 145102.png" alt="屏幕截图 2025-04-02 145102" style="zoom:25%;" /><img src="C:\Users\25685\Pictures\Screenshots\屏幕截图 2025-04-02 145111.png" alt="屏幕截图 2025-04-02 145111" style="zoom:25%;" />

### 主要功能
- 自然语言查询大棚环境数据（温度、湿度、光照等）
- 实时监控环境数据变化
- 远程控制设备（灌溉系统、温控设备等）
- 查看历史数据和趋势图表
- 设置自动控制规则和阈值

## 技术栈

- 微信小程序原生开发框架
- WXML + WXSS + JavaScript
- ECharts小程序版（用于数据可视化）
- WebSocket（与后端实时通信）
- WeUI组件库（UI组件）

## 项目结构

```
miniprogram-1/
├── app.js                  # 小程序入口文件
├── app.json                # 全局配置
├── app.wxss                # 全局样式
├── project.config.json     # 项目配置文件
├── sitemap.json            # 小程序索引配置
├── components/             # 自定义组件
│   ├── chat-box/           # 聊天对话框组件
│   ├── data-chart/         # 数据图表组件
│   ├── device-control/     # 设备控制组件
│   └── loading/            # 加载动画组件
├── pages/                  # 页面
│   ├── index/              # 首页
│   ├── dashboard/          # 数据看板
│   ├── device/             # 设备控制
│   ├── history/            # 历史数据
│   └── settings/           # 设置页面
├── services/               # 服务模块
│   ├── api.js              # API请求封装
│   ├── websocket.js        # WebSocket通信封装
│   └── formatter.js        # 数据格式化工具
├── utils/                  # 工具函数
│   ├── util.js             # 通用工具函数
│   └── auth.js             # 用户认证相关
└── assets/                 # 静态资源
    ├── images/             # 图片资源
    └── icons/              # 图标资源
```

## 页面设计

### 首页 (index)
- 主要功能：提供聊天界面，支持自然语言提问
- 关键元素：
  - 聊天输入框
  - 消息展示区域
  - 快捷提问按钮
  - 语音输入按钮

### 数据看板 (dashboard)
- 主要功能：展示实时环境数据和图表
- 关键元素：
  - 实时温湿度卡片
  - 环境数据趋势图
  - 设备状态概览
  - 数据刷新按钮

### 设备控制 (device)
- 主要功能：控制大棚内设备
- 关键元素：
  - 设备列表
  - 操作按钮（开/关/调节）
  - 自动控制规则设置
  - 设备状态指示

### 历史数据 (history)
- 主要功能：查看历史数据记录和分析
- 关键元素：
  - 日期选择器
  - 历史数据图表
  - 数据导出功能
  - 数据筛选选项

### 设置页面 (settings)
- 主要功能：用户设置和配置
- 关键元素：
  - 消息推送设置
  - 阈值警报设置
  - 用户信息管理
  - 关于和帮助

## 组件设计

### 聊天对话框组件 (chat-box)
```javascript
// 组件属性
properties: {
  messages: Array,  // 聊天消息列表
  loading: Boolean  // 是否显示加载状态
}

// 组件方法
methods: {
  sendMessage(message),  // 发送消息
  clearMessages()        // 清空消息
}
```

### 数据图表组件 (data-chart)
```javascript
// 组件属性
properties: {
  chartType: String,  // 图表类型：line, bar, pie
  chartData: Object,  // 图表数据
  chartOptions: Object // 图表配置
}

// 组件方法
methods: {
  refreshChart(),     // 刷新图表
  exportChart()       // 导出图表数据
}
```

### 设备控制组件 (device-control)
```javascript
// 组件属性
properties: {
  deviceId: String,   // 设备ID
  deviceType: String, // 设备类型
  deviceStatus: Object // 设备状态
}

// 组件方法
methods: {
  toggleDevice(action),   // 控制设备开关
  adjustDevice(params),   // 调整设备参数
  getDeviceStatus()       // 获取设备状态
}
```

## WebSocket通信

### 连接建立
```javascript
// services/websocket.js
const connectWebSocket = () => {
  const ws = wx.connectSocket({
    url: 'wss://your-api-domain.com/ws',
    header: {
      'content-type': 'application/json',
      'Authorization': `Bearer ${getToken()}`
    }
  });
  
  ws.onOpen(() => {
    console.log('WebSocket连接已建立');
    // 发送身份验证消息
    sendMessage({
      type: 'auth',
      data: { token: getToken() }
    });
  });
  
  return ws;
};
```

### 消息处理
```javascript
// 消息接收处理
ws.onMessage((res) => {
  const data = JSON.parse(res.data);
  
  switch (data.type) {
    case 'query_response':
      // 处理查询响应
      handleQueryResponse(data.content);
      break;
    case 'data_update':
      // 处理数据更新
      handleDataUpdate(data.content);
      break;
    case 'device_status':
      // 处理设备状态更新
      handleDeviceStatus(data.content);
      break;
    default:
      console.log('未知消息类型', data);
  }
});
```

### 消息发送
```javascript
// 发送查询消息
const sendQuery = (query) => {
  sendMessage({
    type: 'query',
    data: {
      content: query,
      session_id: getCurrentSessionId()
    }
  });
};

// 发送设备控制命令
const sendDeviceControl = (deviceId, action, params = {}) => {
  sendMessage({
    type: 'device_control',
    data: {
      device_id: deviceId,
      action: action,
      parameters: params
    }
  });
};
```

## API接口文档

### 1. 用户认证接口

#### 登录接口
- 请求方式：POST
- 接口地址：/api/auth/login
- 请求参数：
  ```json
  {
    "code": "wx.login获取的code"
  }
  ```
- 响应数据：
  ```json
  {
    "token": "用户认证令牌",
    "expires_in": 7200
  }
  ```

### 2. 环境数据接口

#### 获取实时数据
- 请求方式：GET
- 接口地址：/api/data/current
- 请求参数：无
- 响应数据：
  ```json
  {
    "temperature": 25.5,
    "humidity": 65.8,
    "light": 6500,
    "soil_moisture": 78.3,
    "co2": 450,
    "timestamp": "2023-05-01T12:30:45Z"
  }
  ```

#### 获取历史数据
- 请求方式：GET
- 接口地址：/api/data/history
- 请求参数：
  ```
  start_time: "2023-04-25T00:00:00Z"
  end_time: "2023-05-01T23:59:59Z"
  data_type: "temperature,humidity"  // 可选，不传则返回所有类型
  interval: "hour"  // 可选，聚合间隔：minute, hour, day
  ```
- 响应数据：
  ```json
  {
    "temperature": [
      {"timestamp": "2023-04-25T01:00:00Z", "value": 23.5},
      {"timestamp": "2023-04-25T02:00:00Z", "value": 23.8},
      // ...
    ],
    "humidity": [
      {"timestamp": "2023-04-25T01:00:00Z", "value": 68.5},
      {"timestamp": "2023-04-25T02:00:00Z", "value": 69.2},
      // ...
    ]
  }
  ```

### 3. 设备控制接口

#### 获取设备列表
- 请求方式：GET
- 接口地址：/api/devices
- 请求参数：无
- 响应数据：
  ```json
  [
    {
      "id": "device001",
      "name": "灌溉系统",
      "type": "irrigation",
      "status": "off",
      "auto_mode": true
    },
    {
      "id": "device002",
      "name": "风扇",
      "type": "fan",
      "status": "on",
      "speed": 2,
      "auto_mode": false
    }
  ]
  ```

#### 控制设备
- 请求方式：POST
- 接口地址：/api/devices/control
- 请求参数：
  ```json
  {
    "device_id": "device001",
    "action": "on",
    "parameters": {
      "duration": 600  // 可选参数，根据设备类型不同而不同
    }
  }
  ```
- 响应数据：
  ```json
  {
    "success": true,
    "device_id": "device001",
    "status": "on",
    "message": "设备已开启"
  }
  ```

## 开发规范

### 命名规范
- 文件命名：小写字母，多个单词用连字符（-）连接
- 变量命名：驼峰命名法（camelCase）
- 常量命名：全大写，单词间用下划线（_）连接
- 组件命名：大驼峰命名法（PascalCase）

### 代码风格
- 缩进：2个空格
- 使用ES6+语法特性
- 每个函数添加适当的注释
- 复杂逻辑需添加详细注释说明

### Git工作流
- feature分支：用于开发新功能
- bugfix分支：用于修复bug
- develop分支：开发环境主分支
- master分支：生产环境主分支

## 部署说明

### 开发环境配置
1. 下载并安装[微信开发者工具](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html)
2. 导入项目到开发者工具
3. 在project.config.json中配置appid

### 测试与调试
1. 在开发者工具中进行本地调试
2. 使用真机调试功能在真实设备上测试
3. 使用开发者工具提供的性能分析工具优化性能

### 发布流程
1. 代码审核通过后合并到master分支
2. 使用开发者工具上传代码
3. 在微信公众平台提交审核
4. 审核通过后发布上线 