小程序端：
用户提问，最近10天空气温湿度如何(小程序需要和后端建立websocket)

后端模块：
把提问请求发送给Agent端模块

Agent端模块：
把用户的提问请求一次大模型API，让大模型选择工具调用。

Agent端模块：
选择工具调用，注意接口传参，例如获取空气温湿度，使用后端的接口即可。

后端模块：
查询数据库，返回给Agent端模块。

agent端模块：
拿到后端返回数据后再次请求大模型厂商API获取回复。需要使用流式输出（SSE链接）

后端模块：
流式传输到小程序端渲染回复内容


# 更新后的系统架构设计

## 整体架构

采用分层设计，所有模块在一个Python程序中实现，通过接口调用实现模块间通信：

```
┌───────────────────────────────────────────────────────────────────────────┐
│                            Python应用程序                                  │
│                                                                           │
│  ┌─────────────┐    ┌──────────────┐    ┌────────────┐                   │
│  │  API服务层  │────│  Agent服务层  │────│ 数据服务层 │───┐               │
│  └──────┬──────┘    └──────────────┘    └────────────┘   │               │
│         │                  │                  │           │               │
│         │                  │                  │           │               │
│         │                  │                  │           │               │
│  ┌──────▼──────┐    ┌─────▼─────┐      ┌─────▼────┐      │               │
│  │ 硬件控制模块 │    │ 大模型接口 │      │ 数据模型 │      │               │
│  └──────┬──────┘    └───────────┘      └──────────┘      │               │
│         │                                                │               │
│         │           ┌─────────────────┐                  │               │
│         │           │   数据采集模块   │◄─────MQTT订阅────┘               │
│         │           └────────┬────────┘                                  │
│         │                    │                                           │
│         │                    │                                           │
│         │                    │                                           │
└─────────┼────────────────────┼───────────────────────────────────────────┘
          │                    │
          ▼                    │
┌─────────────────────┐        │
│    MQTT服务器       │        │
└─────────┬───────────┘        │
          │                    │
          │                    ▼
          │         ┌─────────────────────┐
          │         │       数据库        │
          ▼         └─────────────────────┘
┌─────────────────┐            ▲
│   硬件设备      │            │
└─────────────────┘────────────┘
```

## 模块职责

### 1. API服务层
- 负责与小程序的WebSocket通信
- 接收用户请求并转发给Agent服务层
- 将Agent服务层的流式响应传回小程序
- 管理会话和连接状态
- 接收用户硬件控制指令并转发给硬件控制模块

### 2. Agent服务层
- 封装大模型API调用逻辑
- 实现工具选择和调用功能
- 通过接口调用数据服务层获取所需数据
- 处理流式输出，并通过回调传给API服务层
- 可根据分析结果向API服务层建议硬件控制操作

### 3. 数据服务层
- 封装数据库操作
- 提供数据查询和处理接口
- 实现数据缓存和优化策略
- 返回格式化的数据给Agent服务层
- 管理所有模块对数据库的访问

### 4. 数据采集模块
- 通过MQTT协议订阅传感器数据主题
- 实时接收各类传感器数据（温度、湿度、光照等）
- 对接收的数据进行初步处理和验证
- 将处理后的数据通过数据服务层接口存储到数据库
- 支持多种传感器数据格式解析

### 5. 硬件控制模块
- 提供硬件控制接口供API服务层调用
- 接收来自小程序的用户控制指令
- 支持基于阈值的自动控制规则设置
- 通过MQTT协议发布控制指令到相应主题
- 支持灌溉系统、温室环境控制等多种设备控制
- 实现控制指令的定时和条件触发
- 提供设备状态反馈和监控机制

## 模块间接口设计

### API服务层 → Agent服务层接口
```python
def process_query(query: str, session_id: str, callback) -> None:
    """
    处理用户查询
    
    参数:
        query: 用户查询内容
        session_id: 会话ID
        callback: 回调函数，用于接收流式响应
    """
```

### Agent服务层 → 数据服务层接口
```python
def get_temperature_humidity(days: int = 10) -> List[Dict]:
    """
    获取指定天数的温湿度数据
    
    参数:
        days: 查询的天数
    返回:
        温湿度数据列表
    """
```

### 数据采集模块 → 数据服务层接口
```python
def save_sensor_data(sensor_data: Dict) -> bool:
    """
    保存传感器数据
    
    参数:
        sensor_data: 传感器数据字典，包含传感器ID、类型、数值和时间戳等
    返回:
        保存成功返回True，否则返回False
    """
```

### API服务层 → 硬件控制模块接口
```python
def control_device(device_id: str, action: str, parameters: Dict = None) -> Dict:
    """
    控制设备执行特定动作
    
    参数:
        device_id: 设备ID
        action: 操作类型，如"开启灌溉"、"调节温度"等
        parameters: 操作参数，如持续时间、目标值等
    返回:
        操作结果和状态信息
    """
```

# WebSocket实时数据传输技术架构

## 整体架构

WebSocket实时数据传输采用了多层解耦设计，将数据采集、处理与传输分离，实现高效低延迟的数据推送：

```
┌─────────────┐      ┌───────────────┐      ┌─────────────┐      ┌─────────────┐
│  传感器设备  │───►  │  MQTT服务器   │───►  │ MQTT采集器  │───►  │ 数据服务层  │
└─────────────┘      └───────────────┘      └──────┬──────┘      └──────┬──────┘
                                                    │                    │
                                                    │                    │
                                                    ▼                    ▼
                                            ┌──────────────────────────────────┐
                                            │         消息代理层               │
                                            │  (在MQTT采集器中实现)            │
                                            └──────────────┬───────────────────┘
                                                           │
                                                           │
                                                           ▼
                                            ┌──────────────────────────────────┐
                                            │       WebSocket服务              │
                                            └──────────────┬───────────────────┘
                                                           │
                                                           │
                                                           ▼
                     ┌─────────────────────────────────────────────────────────────┐
                     │                        小程序前端                            │
                     └─────────────────────────────────────────────────────────────┘
```

## 数据流程逻辑

### 1. 数据采集阶段
1. 传感器设备定期采集环境数据（空气温湿度、土壤湿度等）
2. 设备通过MQTT协议将数据发布到特定主题（如`air_sensor/data`、`soil_sensor/data`）
3. MQTT服务器接收发布的数据并转发给订阅者

### 2. 数据处理阶段
1. MQTT采集器（`collector.py`）订阅相关主题，接收传感器数据
2. 采集器解析数据，识别数据类型（空气传感器/土壤传感器）
3. 采集器通过数据服务层（`service.py`）将数据保存到数据库
4. 同时，采集器在内存中缓存最新数据，用于快速响应WebSocket请求

### 3. 数据传输阶段
1. 小程序通过WebSocket连接（`/ws/sensor-data/{data_type}`）订阅特定类型的数据
2. WebSocket服务（`websocket.py`）接受连接，并注册到MQTT采集器
3. 当有新数据到达时，采集器通过内部消息代理机制向相关WebSocket连接广播数据
4. WebSocket服务将数据推送给前端，前端实时更新显示

## 关键组件详解

### MQTT采集器（消息代理层）

MQTT采集器是整个实时数据传输架构的核心，它不仅负责从MQTT服务器获取数据，还承担了消息代理的职责：

```python
class MQTTDataCollector:
    # 初始化连接和数据结构
    def __init__(self):
        # 配置MQTT客户端
        self.client = mqtt.Client(...)
        
        # WebSocket连接管理
        self.active_connections = {
            "all": [],           # 订阅所有数据的连接
            "air_sensor": [],    # 只订阅空气传感器数据的连接
            "soil_sensor": []    # 只订阅土壤传感器数据的连接
        }
        
        # 最新数据缓存
        self.latest_data = {
            "air_sensor": None,
            "soil_sensor": None
        }
    
    # 处理MQTT消息
    def _on_message(self, client, userdata, msg):
        # 解析数据
        # 识别数据类型
        # 保存到数据库
        # 更新缓存
        # 广播到WebSocket
        asyncio.create_task(self.broadcast_message(data, data_type))
    
    # WebSocket客户端管理
    async def register_websocket(self, websocket, data_type):
        # 添加到连接列表
        # 发送最新数据
    
    def unregister_websocket(self, websocket):
        # 从连接列表移除
    
    # 消息广播
    async def broadcast_message(self, message, data_type):
        # 向特定连接列表发送消息
```

### WebSocket服务

WebSocket服务是前端与后端实时通信的桥梁，负责：

1. 建立和维护WebSocket连接
2. 认证用户（可选）
3. 处理前端订阅请求
4. 在MQTT采集器中注册/注销连接
5. 实现心跳检测机制

```python
@router.websocket("/ws/sensor-data/{data_type}")
async def websocket_sensor_data(websocket, data_type, token=None):
    # 验证数据类型
    # 尝试认证用户（可选）
    # 接受连接
    await websocket.accept()
    
    # 发送初始数据
    # 优先使用采集器缓存，其次从数据库获取
    
    # 注册到MQTT采集器
    await mqtt_collector.register_websocket(websocket, data_type)
    
    # 心跳检测循环
    while True:
        data = await websocket.receive_text()
        if data == "ping":
            await websocket.send_text("pong")
```

### 数据服务层

数据服务层负责数据的持久化和查询：

1. 保存传感器数据到数据库
2. 提供历史数据查询
3. 支持数据聚合和统计

```python
class DataService:
    def __init__(self, db):
        self.db = db
    
    def save_sensor_data(self, data, data_type, topic):
        # 根据类型选择不同处理方法
        if data_type == "air_sensor":
            return self._save_air_sensor_data(data)
        elif data_type == "soil_sensor":
            return self._save_soil_humidity_data(data)
    
    def get_latest_sensor_data(self, data_type):
        # 从数据库获取最新数据
        # 格式化为API响应格式
```

## 前端实现

### 连接建立

小程序前端通过以下步骤建立WebSocket连接：

```javascript
// 创建WebSocket连接
const wsUrl = `wss://您的域名/api/v1/ws/sensor-data/${dataType}`;

// 如果有认证令牌
if (token) {
  wsUrl += `?token=${token}`;
}

// 建立连接
const ws = wx.connectSocket({
  url: wsUrl
});

// 设置事件监听
wx.onSocketOpen(handleOpen);
wx.onSocketMessage(handleMessage);
wx.onSocketClose(handleClose);
wx.onSocketError(handleError);
```

### 数据处理

前端接收到数据后进行处理和显示：

```javascript
function handleMessage(res) {
  const data = JSON.parse(res.data);
  
  // 根据数据类型更新UI
  if (data.device_type === 'air_sensor') {
    // 更新空气温湿度显示
    updateAirDisplay(data);
  } else if (data.device_type === 'soil_sensor') {
    // 更新土壤湿度显示
    updateSoilDisplay(data);
  }
}
```

### 心跳机制

为保持连接活跃，前端定期发送心跳：

```javascript
// 定时发送心跳
setInterval(() => {
  if (wsConnected) {
    wx.sendSocketMessage({
      data: 'ping'
    });
  }
}, 30000);
```

## 优化策略

### 数据同步策略

1. **缓存优先**：优先使用MQTT采集器缓存的最新数据
2. **数据库备选**：缓存无数据时从数据库查询
3. **增量更新**：只推送变化的数据，减少带宽占用

### 可靠性保障

1. **断线重连**：前端和后端都实现断线自动重连
2. **心跳检测**：定期心跳检测保持连接活跃
3. **错误隔离**：单一连接错误不影响其他连接

### 性能优化

1. **消息过滤**：根据订阅类型过滤消息
2. **连接池管理**：高效管理多个WebSocket连接
3. **内存缓存**：减少数据库查询次数

## 安全性考虑

1. **可选认证**：支持JWT令牌认证，但非强制（部分数据可公开访问）
2. **数据过滤**：根据用户权限过滤敏感数据
3. **连接限制**：可实现IP和连接数限制防止滥用

## 扩展性设计

1. **数据类型扩展**：轻松添加新的传感器类型
2. **订阅模式扩展**：支持更细粒度的订阅（如特定设备）
3. **消息格式扩展**：标准化的消息格式便于扩展


